# cfd_zoner.py dependencies (tested versions in comments; Python >= 3.10 required)
numpy>=1.24          # tested 2.5.1
scipy>=1.10          # tested 1.18.0 (ndimage: connected components, dilation)
matplotlib>=3.7      # tested 3.10.9 (plots; turbo colormap)
scikit-learn>=1.3    # tested 1.9.0 (k-means clustering, silhouette auto-k)
pyvista>=0.43        # tested 0.48.4 (VTI/STL I/O, 3D rendering)
vtk>=9.2             # tested 9.6.2 (installed by pyvista; pinned for clarity)

# optional: only needed for interactive 3D HTML export (pyvista export_html),
# i.e. --html and the batch comparison zones_3d.html
trame>=3.5
trame-vtk>=2.8
trame-vuetify>=2.4
nest_asyncio2>=1.7   # lets export_html run outside an async event loop
